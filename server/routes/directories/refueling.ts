import type { Express } from "express";

// Old refueling routes are deprecated
// Use unified /api/directories/suppliers and /api/directories/bases instead
export function registerRefuelingDirectoryRoutes(app: Express) {
  // Routes removed - use unified endpoints
}